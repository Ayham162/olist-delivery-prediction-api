# Runtime dependencies only — what the service needs to actually serve
# predictions in a container. Nothing here is needed for lint/test/dev.
# Every version below was installed and smoke-tested together in this repo's
# .venv, not guessed — see TASK3_CHECKLIST.md step 0/§1 for how the exact
# scikit-learn pin was discovered (model.joblib was pickled with 1.8.0;
# an older sklearn silently... actually hard-errors on predict_proba,
# which is the better failure mode, but a failure either way).

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5

# ML / inference — scikit-learn MUST match the version model.joblib and
# preprocessor.joblib were pickled with, or predict_proba breaks outright.
scikit-learn==1.8.0
pandas==2.1.4
numpy==1.26.4
joblib==1.6.0

# Config / env
PyYAML==6.0.3
python-dotenv==1.0.0

# Data versioning, validation, tracking, registry
dvc==3.67.1
great-expectations==1.23.1
mlflow==3.16.1

# Prediction logging (Postgres) + service metrics
psycopg2-binary==2.9.13
prometheus-fastapi-instrumentator==8.1.0
